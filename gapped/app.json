{
  "expo": {
    "name": "Gapped",
    "slug": "gapped",
    "version": "0.1.0",
    "scheme": "gapped",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "dark",
    "backgroundColor": "#0A0A0A",
    "ios": {
      "bundleIdentifier": "app.gapped.drive",
      "supportsTablet": false,
      "infoPlist": {
        "UIBackgroundModes": [
          "location"
        ],
        "NSLocationWhenInUseUsageDescription": "Gapped uses your location to record your drives — speed, route, and distance.",
        "NSLocationAlwaysAndWhenInUseUsageDescription": "Always access lets Gapped keep recording a drive when the app is in the background. Without it we can only track while the app is open.",
        "NSLocationAlwaysUsageDescription": "Always access lets Gapped keep recording a drive when the app is in the background. Without it we can only track while the app is open.",
        "NSMotionUsageDescription": "Motion sensor data is recorded alongside GPS to verify that your runs are real."
      }
    },
    "android": {
      "package": "app.gapped.drive",
      "adaptiveIcon": {
        "backgroundColor": "#0A0A0A",
        "foregroundImage": "./assets/android-icon-foreground.png",
        "backgroundImage": "./assets/android-icon-background.png",
        "monochromeImage": "./assets/android-icon-monochrome.png"
      },
      "permissions": [
        "ACCESS_COARSE_LOCATION",
        "ACCESS_FINE_LOCATION",
        "ACCESS_BACKGROUND_LOCATION",
        "FOREGROUND_SERVICE",
        "FOREGROUND_SERVICE_LOCATION",
        "ACTIVITY_RECOGNITION"
      ],
      "predictiveBackGestureEnabled": false
    },
    "web": {
      "favicon": "./assets/favicon.png"
    },
    "plugins": [
      "expo-router",
      "expo-sqlite",
      [
        "expo-location",
        {
          "locationAlwaysAndWhenInUsePermission": "Always access lets Gapped keep recording a drive when the app is in the background. Without it we can only track while the app is open.",
          "isAndroidBackgroundLocationEnabled": true,
          "isAndroidForegroundServiceEnabled": true
        }
      ],
      "expo-sharing"
    ],
    "experiments": {
      "typedRoutes": true
    }
  }
}
